---
name: xiaohongshu-writer
description: 小红书内容创作和发布流程。当需要写小红书笔记、分析爆款内容、优化文案时使用。
---

# 小红书写作 Skill

## 何时使用
- 创作小红书图文/视频笔记
- 分析热门内容找灵感
- 优化标题和文案
- 发布内容

---

## ⚠️ 发布前必检清单（最重要！）

> **每次发布前必须执行以下检查，避免发布失败！**

### 1. 标题字数检查（最常见错误！）
```
小红书标题限制：最多 20 个字符（含标点符号！）
```

**计数规则**：
- 中文字符 = 1
- 英文字母 = 1
- 数字 = 1
- 标点符号 = 1（包括全角 ｜！？）

**发布前必须数字数，超过 20 就截断！**

| 示例 | 字数 | 结果 |
|-----|-----|------|
| Claude+Zotero+飞书整理论文｜保姆级教程来了！ | 28 | ❌ 超限 |
| Claude+Zotero+飞书｜保姆级教程 | 19 | ✅ OK |

### 2. 登录状态检查
```
调用 check_login_status 确认已登录
如果未登录，调用 get_login_qrcode 获取二维码
```

### 3. 图片路径检查
```bash
# 确认文件存在
ls -la [图片路径]
```
- 使用**绝对路径**：`/Users/xxx/Desktop/xxx.png`
- 至少 1 张，最多 9 张
- 支持 PNG、JPG 格式

### 4. 文案确认
```
⚠️ 必须让用户确认后再发布！
```
- 展示完整预览（标题 + 正文 + 话题）
- 等用户说"确认"或"发布"后才执行
- **不要自动发布！**

---

## 发布失败处理

### 常见错误及解决方案

| 错误信息 | 原因 | 解决方案 |
|---------|------|---------|
| `标题长度超过限制` | 标题 > 20 字 | 缩短标题重试 |
| `Node is detached` | 浏览器页面状态异常 | 调用 check_login_status 刷新后重试 |
| `工具不存在` | MCP 服务重启后会话未刷新 | **重启 Claude Code** |
| `图片文件不存在` | 路径错误或文件不存在 | 检查绝对路径 |
| `未登录` | Cookie 过期 | 重新扫码登录 |
| `内部错误` | 网络或服务问题 | 等待 2 秒后重试 |

### 自动重试流程
```
遇到错误时：
1. 调用 check_login_status 刷新页面状态
2. 等待 2 秒
3. 重新调用 publish_content
4. 如果仍失败，告知用户具体错误
```

---

## 完整发布流程

```
1. 【准备阶段】
   ├── 写好文案（标题 + 正文 + 话题）
   ├── 准备好图片（本地绝对路径）
   └── 建议：保存文案到文件（防止会话丢失）

2. 【检查阶段】（必做！）
   ├── check_login_status（确认登录）
   ├── 检查标题字数 ≤ 20
   ├── ls 检查图片路径存在
   └── 展示预览给用户确认

3. 【发布阶段】
   ├── 用户确认后调用 publish_content
   ├── 如遇 Node detached 错误，自动重试 1 次
   └── 返回发布结果

4. 【后续操作】
   └── 提醒用户：
       - 去 APP 查看发布结果
       - 置顶评论（如有引流需求）
       - 删除之前的错误发布（如有）
```

---

## 文案保存建议

> 防止会话中断导致文案丢失！

建议保存位置：
```bash
~/Desktop/小红书草稿/[日期]-[主题].md
```

文件模板：
```markdown
# 小红书发布草稿

## 标题（20字以内！当前：X字）
xxx

## 正文
xxx

## 话题标签
#xxx #xxx #xxx

## 图片路径
- /Users/xxx/Desktop/xxx/01.png
- /Users/xxx/Desktop/xxx/02.png
```

---

## 文案去AI痕迹（重要！）

> 小红书会对AI生成内容限流，必须人工化处理

**避免这些AI特征**：
- ❌ 整齐的【】标题格式
- ❌ "研究生/科研党必看"等营销套话
- ❌ 过于工整的 emoji 排列
- ❌ "让我来教你"、"接下来我将"等书面语
- ❌ 每段都用 emoji 开头
- ❌ 过于完美的结构（开头-中间-结尾）

**要有的真人感**：
- ✅ 口语化："来还债了hh"、"就是xxx"、"其实不难"
- ✅ 不完美："有点繁琐"、"不会也没事"
- ✅ 随意的标点：少用感叹号，多用逗号
- ✅ 偶尔的错别字或网络用语
- ✅ 第一人称经验分享："我现在xxx"、"我之前踩过坑"
- ✅ 自然的互动："有问题直接问"、"看到都会回"

**改写示例**：
```
❌ AI风格：
【Step 1: 安装 Claude Code】
打开终端输入以下命令：

✅ 真人风格：
配置其实不难，就是有点繁琐
具体步骤我放图里了，跟着做就行
```

---

## 爆款内容公式

### 标题公式 (必须 ≤ 20 字！)
1. **数字型**: "5个技巧让你..."、"90%的人不知道..."
2. **疑问型**: "为什么...？"、"如何...？"
3. **对比型**: "xxx vs xxx，谁更值得买？"
4. **情绪型**: "绝了！"、"救命！"、"yyds！"
5. **人群型**: "打工人必看"、"学生党福音"

### 正文结构
```
开头（吸引注意）
- 痛点共鸣 / 结果展示 / 悬念设置

中间（价值输出）
- 分点清晰
- 每点一个核心信息
- 配合 emoji 分隔

结尾（引导互动）
- 总结要点
- 引导收藏/评论
- 相关话题标签
```

### Emoji 使用规范
- 每段开头用 emoji 作为视觉锚点
- 重点信息前加强调 emoji
- 不要过度堆砌（每100字2-3个）
- 常用：✨💡🔥❗️📌💰🎯

---

## 不同类型内容模板

### 干货教程类
```
【标题】小白也能学会的xxx教程

第一步：xxx ✨
[具体操作]

第二步：xxx 💡
[具体操作]

第三步：xxx 🎯
[具体操作]

⚠️ 避坑提醒：
- xxx
- xxx

💬 有问题评论区问我~
记得收藏防丢失 📌
```

### 测评种草类
```
【标题】用了xxx，我哭了😭

💰 价格：xxx
📦 购买渠道：xxx

✅ 优点：
- xxx
- xxx

❌ 缺点：
- xxx

🎯 适合人群：xxx
⭐ 总结：x分/10分

你们用过吗？评论区聊聊~
```

### 经验分享类
```
【标题】从xxx到xxx，我总结了这x点

背景：[简述个人经历]

📌 经验1：xxx
[展开说明]

📌 经验2：xxx
[展开说明]

📌 经验3：xxx
[展开说明]

希望对你有帮助！
有问题可以问我~
```

---

## 热门话题标签

### 通用标签
- #日常分享 #生活记录 #干货分享

### 领域标签
根据内容添加垂直领域标签，提高精准推荐

---

## 违规内容检查

发布前检查是否包含违规内容：
- ❌ 第三方平台链接、二维码、口令码
- ❌ "评论区发链接"等引流话术
- ❌ 第三方 logo/水印/名称
- ❌ 引导用户前往其他平台

**安全的引流方式**：
- ✅ "评论区扣1，我私发给你"
- ✅ "G站搜 xxx"（暗示 GitHub）
- ✅ 个人主页设置外链

---

## 发布时间建议

最佳发布时间段：
- 早 8:00-9:00（通勤时间）
- 午 12:00-14:00（午休时间）
- 晚 20:00-22:00（睡前刷手机）

---

## 使用 xiaohongshu MCP

```bash
# 1. 检查登录状态（发布前必做！）
check_login_status

# 2. 搜索热门内容找灵感
search_feeds: keyword="关键词"

# 3. 分析高赞内容
get_feed_detail: feed_id, xsec_token

# 4. 发布图文（图片用绝对路径）
publish_content:
  title: "标题"  # ≤ 20 字！
  content: "正文"
  images: ["/Users/xxx/xxx.png"]  # 绝对路径
  tags: ["话题1", "话题2"]

# 5. 发布视频
publish_with_video:
  title: "标题"
  content: "正文"
  video: "/Users/xxx/xxx.mp4"
```

---

## 数据分析指标

评估内容质量：
- 点赞数 > 500：不错
- 收藏数 > 300：实用价值高
- 评论数 > 50：话题性强
- 收藏/点赞比 > 50%：干货型内容
